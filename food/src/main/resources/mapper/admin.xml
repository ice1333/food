<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin"> <!-- mapper파일의 이름 -->
	
	<select id="login" parameterType="admin.AdminVo" resultType="admin.AdminVo">
		SELECT * FROM admin WHERE A_id = #{A_id} AND A_pw = md5(#{A_pw})
	</select>

	<select id="userList" parameterType="admin.UserVo" resultType="admin.UserVo">
		SELECT * FROM user
		<include refid = "boardWhere"/>
		ORDER BY u_regdate DESC    
		LIMIT ${startIdx},10
	</select>
	
	<sql id = "boardWhere">
		<where>
			<if test ="searchType ==''">   
			 	(u_uemail LIKE '%${searchWord}%' OR u_status LIKE '%${searchWord}%')
			 </if>
			 <if test= "searchType != null and searchType != ''">
			 	${searchType} LIKE '%${searchWord}%'
			 </if>
			 <if test="u_no > 0">    
			 	AND u_no = ${u_no}
			 </if>
		</where>     
	</sql>
	
	<select id="count" resultType="int" parameterType="admin.UserVo">
		SELECT COUNT(*) FROM user
		<include refid="boardWhere"/>
	</select> 
	
	<delete id="delete" >
		DELETE FROM user WHERE u_no=#{u_no}
	</delete>
</mapper>